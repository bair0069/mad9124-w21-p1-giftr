/**
 * TODO:
 * 
 * import mongoose from "mongoose";
 * import validate from "validator";
 * import bcrypt from "bcrypt";
 * 
 * create schema for user
 */

// has to include firstName, lastName, email(***Email needs to be validated***), password(encrypted), 

// schema.pre("save", function (next) { encrypt the password before saving to the database})