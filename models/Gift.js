/**
 * TODO:
 * import mongoose from "mongoose";
 * create schema for Gift
 */

// has to include name, price, imageUrl, store, store.name, store.productUrl

// export default mongoose.model("Gift", schema);